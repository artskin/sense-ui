:host{
  
  .popover{
    width: 10em;
    height :6em;
    position: relative;
    border:none;
    //background:transiton;/*这个属性也可当作Chrome的已完成进度背景，只不过被下面的::progress-bar覆盖了*/
    border-radius: 2.2em;
    display: none;
    overflow: hidden;

    // &::after{
    //   content: var(--progress-value);
    //   display:block;
    //   width: 3.5em;
    //   position: absolute;
    //   right :-3.5em;
    //   text-align: left ;
    //   top:0;
    //   font-size: .5em;
    // }
  }
}

:host([open]) .popover{
  display: block;
}